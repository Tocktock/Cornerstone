# v2.4.0 Release Notes — Connector Expansion and Live-Proof Hardening

`v2.4.0` adds a connector support matrix for operators and downstream consumers.

## Added

- `GET /v1/connectors/support-matrix`
- Per-provider object support state.
- Live-proof guard names.
- Secret-redaction policy for proof artifacts.

## Trust boundary

Connector behavior still cannot mutate the official ontology graph directly.
